# utils

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "SoftwareSourceCode",
  "name": "utils",
  "description": "Directory containing 2 code files with 1 classes and 12 functions",
  "programmingLanguage": [
    {
      "@type": "ComputerLanguage",
      "name": "Python"
    }
  ],
  "featureList": [
    "1 class definitions",
    "12 function definitions"
  ]
}
</script>

## Overview

This directory contains 2 code file(s) with extracted schemas.

## Files and Schemas

### `git_operations.py` (python)

**Functions:**
- `get_git_repos_with_remotes(schemas_file) -> List[...]` - Line 15
- `git_status(repo_path) -> Tuple[...]` - Line 28
- `git_add_all(repo_path) -> bool` - Line 43
- `git_commit(repo_path, message) -> bool` - Line 51
- `git_push(repo_path) -> Tuple[...]` - Line 64
- `main()` - Line 78

**Key Imports:** `json`, `logging`, `pathlib`, `subprocess`, `typing`

### `logging_config.py` (python)

**Classes:**
- `ColoredFormatter` (extends: logging.Formatter) - Line 42
  - Custom formatter with colored output for console
  - Methods: format

**Functions:**
- `init_sentry(dsn, environment, traces_sample_rate, profiles_sample_rate) -> bool` - Line 70
- `_before_send_filter(event, hint)` - Line 125
- `setup_logging(name, level, log_file, use_colors, structured) -> logging.Logger` - Line 145
- `get_logger(name, level, log_file) -> logging.Logger` - Line 222
- `log_exception(logger, error, context)` - Line 262
- `log_performance_metric(logger, operation, duration_ms, metadata)` - Line 296

**Key Imports:** `datetime`, `logging`, `logging.handlers`, `os`, `pathlib` (+5 more)

---
*Generated by Enhanced Schema Generator with schema.org markup*